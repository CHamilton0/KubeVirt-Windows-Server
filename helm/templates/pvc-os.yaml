{{- if .Values.createOSPVC }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: os-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: {{ .Values.storageClassName }}
  resources:
    requests:
      storage: {{ .Values.storage.osDiskSize }}
{{- end }}
